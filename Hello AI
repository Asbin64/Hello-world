name: Greet OpenAI

on:
  push:
    branches:
      - main  # Replace with your branch name

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello to OpenAI
        run: echo "Hello, OpenAI!"

      - name: Print Current Date
        run: echo "Current date: $(date)"
